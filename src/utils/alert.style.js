import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  toastNotification: {
    height: '3rem',
    borderRadius: '24px',
    padding: '15px',
  },
});
export default useStyles;
