import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  page: {
    display: 'flex',
    flexDirection: 'row',
    height: '100%',
    position: 'absolute',
    top: '0',
  },
  waveBackground: {
    position: 'absolute',
  },
});

export default useStyles;
